#include <iostream>
using namespace std;

int main () {
    int A, B, C, D, E, F;
    double tot;

    cin >> A;
    cin >> B;
    cin >> C;
    cin >> D;
    cin >> E;
    cin >> F;

    tot = ((A+ B) * (C - D) * (E + F)) / 2;

    cout << "Eu sou FERA nas continhas e o resutado e " << tot;

}